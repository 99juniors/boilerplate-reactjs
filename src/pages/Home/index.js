import { Button } from '../../components';

import * as s from './styles';

const HomePage = () => {
  return (
    <s.HomePageWrapper>
      <s.ContentWrapper>
        HOME PAGE
        <Button />
      </s.ContentWrapper>
    </s.HomePageWrapper>
  );
};

export default HomePage;
